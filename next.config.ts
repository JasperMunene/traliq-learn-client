import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ["images.pexels.com", "res.cloudinary.com", "images.unsplash.com", "d3njjcbhbojbot.cloudfront.net", "lh3.googleusercontent.com"],
    },
};

export default nextConfig;
